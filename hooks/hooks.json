{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'FILE=\"${CODEX_FILE_PATH:-${CLAUDE_FILE_PATH:-}}\"; if [[ \"$FILE\" == *.hs ]]; then if command -v hlint >/dev/null 2>&1; then hlint \"$FILE\" 2>&1 | head -20; else echo \"hlint not found - install with: cabal install hlint\"; fi; fi'",
            "statusMessage": "Running HLint"
          }
        ]
      }
    ]
  }
}
